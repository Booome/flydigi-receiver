# Apex5 BT HID 输入映射（实测 — Task 3 产出）

> **此文件在 Task 3（逐键实测）完成前留空**。届时用以下结构填表：
>
> 1. 物理控制 → 字节.位（哪个 report 字节、哪个 bit）
> 2. 摇杆/扳机 → 字节序、量程、符号、有符号/无符号
> 3. 中心/最小/最大实测值

## 抽到的对照样本（连接后空闲态）

- `apex5-hid-descriptor.md` 内已记录：dump 后第一次抓到 `0081ff7f0080ff7f00000000000000`。
- 后续可采集多份样本，按下不同物理按钮+推满摇杆/扳机，逐字节对比增量。

## Task 3 验证步骤

按 `docs/superpowers/plans/2026-09-05-bt-hid-report-decode.md` Task 3 执行：
1. dump descriptor
2. 每个按钮分别按下 + 摇杆 4 方向 + 扳机到底
3. 记录每份帧中变化字节→对应物理控制
4. 字段偏移/字节序/量程/符号与本表交叉
5. 不一致以实测为准
