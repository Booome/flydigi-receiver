# M7 sle_probe 被动接收探测实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建立 `sle_probe` 独立探测 app，被动接收手柄 SLE 数据并 hex 打印，验证数据通道与 payload 格式。

**Architecture:** 公共代码 `common/` 重命名为 `src/`（前置重构）；新建 `apps/sle_probe` app，基于 SDK `sle_uart_client` 示例最小改造：seek 收集全部设备（复用 `src/scan_table`）→ 选 RSSI 最强连接 + 配对 → SSAP find structure 打印服务结构 → notification/indication 回调 hex 打印数据。纯被动接收，不发送数据、不改 default。

**Tech Stack:** Ai-BS21_SDK（SLE/SSAP）、BS21 板载固件（C）、CMake（`BS21_APP` 多 app 构建）

## Global Constraints

- SDK 只读引用模式：不修改 `~/.local/Ai-BS21_SDK` 任何源码。
- `-Werror`，所有编译必须 0 warning。
- 不修改 default / 其他现有 app 的行为逻辑（仅 CMakeLists 路径更新允许）。
- 所有文件修改在 worktree `m7-sle-probe` 中进行（`.worktrees/m7-sle-probe`）。
- 代码注释英文，保持简短（只写 why，不写 what）。
- 每个 task 完成后 run code-simplifier 技能做简化 pass 并保持编译通过。
- 提交信息风格：`<type>: <subject>`（feat/fix/docs/refactor/chore）。

---
## 任务总览

| Task | 内容 | 交付可验证 |
|------|------|-----------|
| 1 | `common/` → `src/` 重构 | default 编译通过，行为不变 |
| 2 | scan_table 增加 `scan_table_best` / `scan_table_reset` | default 编译通过 |
| 3 | sle_probe 骨架（CMakeLists + main.c） | `BS21_APP=sle_probe` 编译通过 |
| 4 | 扫描收集 + 连接/配对（sle_probe_client） | 编译通过；板上能连上手柄 |
| 5 | SSAP 发现 + 数据打印 | 编译通过；板上能收到数据帧 |
| 6 | 板上验证 | log 记录 seek/SSAP/recv 结果 |

---

### Task 1: `common/` → `src/` 重构

**Files:**
- Rename: `wireless/bs21/common/` → `wireless/bs21/src/`
- Modify: `wireless/bs21/apps/default/CMakeLists.txt`
- Modify: `wireless/bs21/apps/g_scanner/CMakeLists.txt`
- Modify: `wireless/bs21/apps/sle_accept/CMakeLists.txt`
- Modify: `wireless/bs21/apps/sle_connect/CMakeLists.txt`
- Modify: `wireless/bs21/apps/sle_pair/CMakeLists.txt`
- Modify: `wireless/bs21/apps/t_broadcaster/CMakeLists.txt`

**Interfaces:**
- Produces: 目录 `wireless/bs21/src/`（含 `bs21_util.c/.h`、`scan_table.c/.h`、`controller_state.h`），6 个 app 的 CMakeLists 全部指向 `src/`。

- [ ] **Step 1: git mv 目录**

```bash
cd /home/bodong/workspace/flydigi-receiver/.worktrees/m7-sle-probe/wireless/bs21
git mv common src
git status --short
```

Expected: `R  common/... -> src/...`（5 个文件重命名）。

- [ ] **Step 2: 更新 6 个 app 的 CMakeLists**

每个文件把源码路径与 include path 中的 `common` 全部改为 `src`：

```bash
cd /home/bodong/workspace/flydigi-receiver/.worktrees/m7-sle-probe/wireless/bs21
sed -i 's#\${CMAKE_SOURCE_DIR}/common#${CMAKE_SOURCE_DIR}/src#g' \
  apps/default/CMakeLists.txt apps/g_scanner/CMakeLists.txt apps/sle_accept/CMakeLists.txt \
  apps/sle_connect/CMakeLists.txt apps/sle_pair/CMakeLists.txt apps/t_broadcaster/CMakeLists.txt
```

- [ ] **Step 3: 确认无残留引用**

```bash
grep -rn "common" /home/bodong/workspace/flydigi-receiver/.worktrees/m7-sle-probe/wireless/bs21/CMakeLists.txt \
  /home/bodong/workspace/flydigi-receiver/.worktrees/m7-sle-probe/wireless/bs21/apps/*/CMakeLists.txt
```

Expected: 无 `common` 匹配（若脚本/sdk-compat 等有引用一并处理）。

- [ ] **Step 4: 编译验证 default**

```bash
cd /home/bodong/workspace/flydigi-receiver/.worktrees/m7-sle-probe
cmake -S wireless/bs21 -B wireless/bs21/build -DBS21_APP=default
cmake --build wireless/bs21/build -j
```

Expected: 编译成功，0 warning（-Werror）。

- [ ] **Step 5: Commit**

```bash
cd /home/bodong/workspace/flydigi-receiver/.worktrees/m7-sle-probe
git add -A
git commit -m "refactor: rename common/ to src/ for shared project source"
```

---

### Task 2: scan_table 增加选最强与重置接口

**Files:**
- Modify: `wireless/bs21/src/scan_table.h`
- Modify: `wireless/bs21/src/scan_table.c`

**Interfaces:**
- Produces:
  - `scan_device_t *scan_table_best(void)` —— 返回扫描表中 RSSI 最大的设备指针，无设备返回 NULL。
  - `void scan_table_reset(void)` —— 清空扫描表（含 `g_table_full` 标志）。

**Consumes:** Task 1 后的 `src/scan_table.c`（`g_scan_table` static 数组、`g_table_full` 标志）。

- [ ] **Step 1: 更新头文件声明**

在 `wireless/bs21/src/scan_table.h` 的 `scan_table_full` 声明前追加两个声明：

```c
scan_device_t *scan_table_best(void);
void scan_table_reset(void);
```

- [ ] **Step 2: 实现函数**

在 `wireless/bs21/src/scan_table.c` 末尾追加：

```c
scan_device_t *scan_table_best(void)
{
    scan_device_t *best = NULL;
    for (uint8_t i = 0; i < SCAN_TABLE_SIZE; i++) {
        if (g_scan_table[i].used &&
            (best == NULL || g_scan_table[i].rssi > best->rssi)) {
            best = &g_scan_table[i];
        }
    }
    return best;
}

void scan_table_reset(void)
{
    for (uint8_t i = 0; i < SCAN_TABLE_SIZE; i++) {
        g_scan_table[i].used = 0;
        g_scan_table[i].count = 0;
        g_scan_table[i].rssi = 0;
    }
    g_table_full = false;
}
```

- [ ] **Step 3: 编译验证 default**

```bash
cd /home/bodong/workspace/flydigi-receiver/.worktrees/m7-sle-probe
cmake --build wireless/bs21/build -j
```

Expected: 编译成功，0 warning。

- [ ] **Step 4: Commit**

```bash
cd /home/bodong/workspace/flydigi-receiver/.worktrees/m7-sle-probe
git add wireless/bs21/src/scan_table.c wireless/bs21/src/scan_table.h
git commit -m "feat: add scan_table_best and scan_table_reset helpers"
```

---

### Task 3: sle_probe app 骨架（CMakeLists + main.c）

**Files:**
- Create: `wireless/bs21/apps/sle_probe/CMakeLists.txt`
- Create: `wireless/bs21/apps/sle_probe/main.c`

**Interfaces:**
- Consumes: `src/bs21_util.c`（`bs21_rst()`）、`src/scan_table.c`、`src` include path。
- Produces: `void probe_init(void)`（Task 4 定义，main.c 调用）。

- [ ] **Step 1: 创建 CMakeLists.txt**

`wireless/bs21/apps/sle_probe/CMakeLists.txt`：

```cmake
set(COMPONENT_NAME "sle_probe")

set(SOURCES
    ${CMAKE_CURRENT_SOURCE_DIR}/main.c
    ${CMAKE_CURRENT_SOURCE_DIR}/sle_probe_client.c
    ${CMAKE_SOURCE_DIR}/src/scan_table.c
    ${CMAKE_SOURCE_DIR}/src/bs21_util.c
)

set(PUBLIC_HEADER
    ${CMAKE_CURRENT_SOURCE_DIR}
    ${CMAKE_SOURCE_DIR}/src
)

set(PRIVATE_HEADER)

set(PRIVATE_DEFINES)

set(PUBLIC_DEFINES)

set(COMPONENT_PUBLIC_CCFLAGS)

set(WHOLE_LINK
true
)

set(MAIN_COMPONENT
false
)

build_component()
```

- [ ] **Step 2: 创建 main.c**

`wireless/bs21/apps/sle_probe/main.c`：

```c
#include "soc_osal.h"
#include "sle_device_manager.h"
#include "bs21_util.h"
#include "sle_probe_client.h"

void axk_main(void)
{
    osal_printk("app: sle-probe\r\n");
    bs21_rst();
    probe_init();
    enable_sle();
}
```

> `sle_probe_client.c` 在 Task 4 创建；Step 3 之前先创建空占位（仅 `#include "sle_probe_client.h"` 与空 `void probe_init(void) {}`），保证本 Task 可编译。

- [ ] **Step 3: 编译验证 sle_probe**

```bash
cd /home/bodong/workspace/flydigi-receiver/.worktrees/m7-sle-probe
cmake -S wireless/bs21 -B wireless/bs21/build-sleprobe -DBS21_APP=sle_probe
cmake --build wireless/bs21/build-sleprobe -j
```

Expected: 编译成功，0 warning，产出 `build-sleprobe/sle_probe/bs21_all_in_one.fwpkg`。

- [ ] **Step 4: Commit**

```bash
cd /home/bodong/workspace/flydigi-receiver/.worktrees/m7-sle-probe
git add wireless/bs21/apps/sle_probe
git commit -m "feat: add sle_probe app skeleton"
```

---

### Task 4: 扫描收集 + 连接/配对

**Files:**
- Create: `wireless/bs21/apps/sle_probe/sle_probe_client.h`
- Create: `wireless/bs21/apps/sle_probe/sle_probe_client.c`（覆盖 Task 3 的占位）

**Interfaces:**
- Consumes: `src/scan_table`（`scan_table_find/add/print/best/reset`）、`src/bs21_util`（`bs21_rst`、`sle_scan_start`）。
- Produces: `void probe_init(void)` —— 注册 dev/seek/conn 回调。

- [ ] **Step 1: 创建 sle_probe_client.h**

```c
#ifndef SLE_PROBE_CLIENT_H
#define SLE_PROBE_CLIENT_H

void probe_init(void);

#endif /* SLE_PROBE_CLIENT_H */
```

- [ ] **Step 2: 创建 sle_probe_client.c**

```c
#include "soc_osal.h"
#include "securec.h"
#include "sle_common.h"
#include "sle_device_manager.h"
#include "sle_device_discovery.h"
#include "sle_connection_manager.h"
#include "scan_table.h"
#include "bs21_util.h"
#include "sle_probe_client.h"

#define PROBE_LOG        "[probe]"
#define PROBE_SCAN_MS    5000

static sle_dev_manager_callbacks_t g_dev_cbk = { 0 };
static sle_announce_seek_callbacks_t g_seek_cbk = { 0 };
static sle_connection_callbacks_t g_conn_cbk = { 0 };
static sle_addr_t g_target_addr = { 0 };
static uint16_t g_conn_id = 0;
static uint32_t g_scan_start_ms = 0;

static void probe_start_scan(void)
{
    g_scan_start_ms = uapi_systick_get_ms();
    sle_scan_start();
}

static void probe_connect_best(void)
{
    scan_device_t *best = scan_table_best();
    if (best == NULL) {
        osal_printk("%s no device found, rescan\r\n", PROBE_LOG);
        scan_table_reset();
        probe_start_scan();
        return;
    }
    memcpy_s(&g_target_addr, sizeof(sle_addr_t), &best->addr, sizeof(sle_addr_t));
    osal_printk("%s pick best: %02x:%02x:%02x:%02x:%02x:%02x rssi=%d\r\n",
                PROBE_LOG,
                best->addr.addr[0], best->addr.addr[1], best->addr.addr[2],
                best->addr.addr[3], best->addr.addr[4], best->addr.addr[5],
                best->rssi);
    sle_remove_paired_remote_device(&g_target_addr);
    sle_connect_remote_device(&g_target_addr);
}

static void probe_power_on_cb(uint8_t status)
{
    osal_printk("%s power on: %d\r\n", PROBE_LOG, status);
    enable_sle();
}

static void probe_enable_cb(uint8_t status)
{
    osal_printk("%s sle enable: %d\r\n", PROBE_LOG, status);
    probe_start_scan();
}

static void probe_seek_enable_cb(errcode_t status)
{
    osal_printk("%s seek enable: 0x%x\r\n", PROBE_LOG, status);
}

static void probe_seek_result_cb(sle_seek_result_info_t *result)
{
    scan_device_t *dev;
    if (result == NULL) {
        return;
    }
    dev = scan_table_find(&result->addr);
    if (dev == NULL) {
        dev = scan_table_add(&result->addr);
    }
    if (dev == NULL) {
        return;
    }
    dev->count++;
    dev->rssi = result->rssi;

    if (uapi_systick_get_ms() - g_scan_start_ms >= PROBE_SCAN_MS) {
        sle_stop_seek();
    }
}

static void probe_seek_disable_cb(errcode_t status)
{
    osal_printk("%s seek disable: 0x%x\r\n", PROBE_LOG, status);
    scan_table_print();
    probe_connect_best();
}

static void probe_conn_state_cb(uint16_t conn_id, const sle_addr_t *addr,
                                sle_acb_state_t state, sle_pair_state_t pair_state,
                                sle_disc_reason_t reason)
{
    (void)addr;
    g_conn_id = conn_id;
    osal_printk("%s conn state: %d pair:%d reason:0x%x\r\n", PROBE_LOG, state, pair_state, reason);
    if (state == SLE_ACB_STATE_CONNECTED) {
        osal_printk("%s connected, conn_id=%u\r\n", PROBE_LOG, conn_id);
        if (pair_state == SLE_PAIR_NONE) {
            sle_pair_remote_device(&g_target_addr);
        }
    } else if (state == SLE_ACB_STATE_DISCONNECTED) {
        scan_table_reset();
        probe_start_scan();
    }
}

static void probe_pair_complete_cb(uint16_t conn_id, const sle_addr_t *addr, errcode_t status)
{
    (void)conn_id;
    (void)addr;
    osal_printk("%s pair complete: 0x%x\r\n", PROBE_LOG, status);
}

void probe_init(void)
{
    g_dev_cbk.sle_power_on_cb = probe_power_on_cb;
    g_dev_cbk.sle_enable_cb = probe_enable_cb;
    sle_dev_manager_register_callbacks(&g_dev_cbk);

    g_seek_cbk.seek_enable_cb = probe_seek_enable_cb;
    g_seek_cbk.seek_result_cb = probe_seek_result_cb;
    g_seek_cbk.seek_disable_cb = probe_seek_disable_cb;
    sle_announce_seek_register_callbacks(&g_seek_cbk);

    g_conn_cbk.connect_state_changed_cb = probe_conn_state_cb;
    g_conn_cbk.pair_complete_cb = probe_pair_complete_cb;
    sle_connection_register_callbacks(&g_conn_cbk);
}
```

- [ ] **Step 3: 编译验证 sle_probe**

```bash
cd /home/bodong/workspace/flydigi-receiver/.worktrees/m7-sle-probe
cmake --build wireless/bs21/build-sleprobe -j
```

Expected: 编译成功，0 warning。

- [ ] **Step 4: Commit**

```bash
cd /home/bodong/workspace/flydigi-receiver/.worktrees/m7-sle-probe
git add wireless/bs21/apps/sle_probe
git commit -m "feat: sle_probe scan collect and connect strongest device"
```

---

### Task 5: SSAP 发现与数据打印

**Files:**
- Modify: `wireless/bs21/apps/sle_probe/sle_probe_client.c`
- Modify: `wireless/bs21/apps/sle_probe/sle_probe_client.h`

**Interfaces:**
- Consumes: Task 4 的 `g_conn_id`、`g_target_addr`、`probe_init`、`probe_pair_complete_cb`。
- Produces: SSAP client 回调（exchange/find/notification/indication），pair 成功后自动开始 find。

- [ ] **Step 1: .c 增加 ssap include**

`wireless/bs21/apps/sle_probe/sle_probe_client.c` 的 include 区追加：

```c
#include "sle_ssap_client.h"
```

（SSAP 类型如 `ssapc_callbacks_t`、`ssap_exchange_info_t`、`ssapc_handle_value_t` 均由此头提供；header 文件无需改动。）

- [ ] **Step 2: 修改 sle_probe_client.c——增加 SSAP 回调与数据打印**

`#define PROBE_LOG ...` 行后追加宏与 static 回调结构：

```c
#define PROBE_MTU_SIZE_DEFAULT 520

static ssapc_callbacks_t g_ssapc_cbk = { 0 };
```

将 `probe_pair_complete_cb` 替换为配对成功后发起 MTU 交换 + find：

```c
static void probe_pair_complete_cb(uint16_t conn_id, const sle_addr_t *addr, errcode_t status)
{
    (void)conn_id;
    (void)addr;
    osal_printk("%s pair complete: 0x%x\r\n", PROBE_LOG, status);
    if (status == 0) {
        ssap_exchange_info_t info = { 0 };
        info.mtu_size = PROBE_MTU_SIZE_DEFAULT;
        info.version = 1;
        ssapc_exchange_info_req(0, g_conn_id, &info);
    }
}

static void probe_exchange_info_cb(uint8_t client_id, uint16_t conn_id,
                                   ssap_exchange_info_t *param, errcode_t status)
{
    (void)client_id;
    (void)conn_id;
    osal_printk("%s exchange info: 0x%x mtu=%u\r\n", PROBE_LOG, status, param->mtu_size);
    ssapc_find_structure_param_t find_param = { 0 };
    find_param.type = SSAP_FIND_TYPE_PROPERTY;
    find_param.start_hdl = 1;
    find_param.end_hdl = 0xFFFF;
    ssapc_find_structure(0, g_conn_id, &find_param);
}

static void probe_find_service_cb(uint8_t client_id, uint16_t conn_id,
                                  ssapc_find_service_result_t *service, errcode_t status)
{
    (void)client_id;
    (void)conn_id;
    osal_printk("%s find service: status=0x%x start=0x%x end=0x%x uuid_len=%u\r\n",
                PROBE_LOG, status, service->start_hdl, service->end_hdl, service->uuid.len);
}

static void probe_find_property_cb(uint8_t client_id, uint16_t conn_id,
                                   ssapc_find_property_result_t *property, errcode_t status)
{
    (void)client_id;
    (void)conn_id;
    osal_printk("%s find property: status=0x%x handle=0x%x oper_ind=%u desc=%u\r\n",
                PROBE_LOG, status, property->handle, property->operate_indication,
                property->descriptors_count);
}

static void probe_find_cmp_cb(uint8_t client_id, uint16_t conn_id,
                              ssapc_find_structure_result_t *result, errcode_t status)
{
    (void)client_id;
    (void)conn_id;
    osal_printk("%s find complete: status=0x%x type=%u uuid_len=%u\r\n",
                PROBE_LOG, status, result->type, result->uuid.len);
}

static void probe_print_hex(const uint8_t *buf, uint32_t len)
{
    for (uint32_t i = 0; i < len; i++) {
        osal_printk("%02x ", buf[i]);
    }
    osal_printk("\r\n");
}

static void probe_notification_cb(uint8_t client_id, uint16_t conn_id,
                                  ssapc_handle_value_t *data, errcode_t status)
{
    (void)client_id;
    (void)conn_id;
    (void)status;
    osal_printk("%s recv len=%u ", PROBE_LOG, data->data_len);
    probe_print_hex(data->data, data->data_len);
}

static void probe_indication_cb(uint8_t client_id, uint16_t conn_id,
                                ssapc_handle_value_t *data, errcode_t status)
{
    (void)client_id;
    (void)conn_id;
    (void)status;
    osal_printk("%s ind len=%u ", PROBE_LOG, data->data_len);
    probe_print_hex(data->data, data->data_len);
}
```

将 `probe_init` 尾部追加 SSAP 回调注册：

```c
    g_ssapc_cbk.exchange_info_cb = probe_exchange_info_cb;
    g_ssapc_cbk.find_structure_cb = probe_find_service_cb;
    g_ssapc_cbk.ssapc_find_property_cbk = probe_find_property_cb;
    g_ssapc_cbk.find_structure_cmp_cb = probe_find_cmp_cb;
    g_ssapc_cbk.notification_cb = probe_notification_cb;
    g_ssapc_cbk.indication_cb = probe_indication_cb;
    ssapc_register_callbacks(&g_ssapc_cbk);
```

- [ ] **Step 3: 编译验证 sle_probe**

```bash
cd /home/bodong/workspace/flydigi-receiver/.worktrees/m7-sle-probe
cmake --build wireless/bs21/build-sleprobe -j
```

Expected: 编译成功，0 warning。

- [ ] **Step 4: Commit**

```bash
cd /home/bodong/workspace/flydigi-receiver/.worktrees/m7-sle-probe
git add wireless/bs21/apps/sle_probe
git commit -m "feat: sle_probe ssap discovery and hex data print"
```

---

### Task 6: 板上验证

**Files:** 无代码改动（记录结果）。

- [ ] **Step 1: 编译 sle_probe 最新**

```bash
cd /home/bodong/workspace/flydigi-receiver/.worktrees/m7-sle-probe
cmake -S wireless/bs21 -B wireless/bs21/build-sleprobe -DBS21_APP=sle_probe
cmake --build wireless/bs21/build-sleprobe -j
```

Expected: 0 warning。

- [ ] **Step 2: 烧录 board_a**

```bash
cd /home/bodong/workspace/flydigi-receiver/.worktrees/m7-sle-probe
fuser -k /dev/ttyUSB6 2>/dev/null; rm -f /var/lock/LCK..ttyUSB6
python3 wireless/bs21/tools/burn.py board_a
```

Expected: 烧录成功，设备正常进入 `app: sle-probe`。

- [ ] **Step 3: 抓 log 并让手柄靠近**

```bash
stty -F /dev/ttyUSB6 115200 raw -echo
cat /dev/ttyUSB6 > /tmp/probe.log &
```

- 手柄切 2.4G（PC）模式开机，靠近 board_a。
- 等待数秒后 `kill %1`。

Expected log 顺序：
1. `[probe] power on` → `[probe] sle enable` → `[probe] seek enable`
2. `[probe] seek result`（经 scan_table）→ `[scan] devices:N` → `[probe] pick best`
3. `[probe] conn state: 1` → `[probe] connected` → `[probe] pair complete: 0x0`
4. `[probe] exchange info` → `[probe] find service/property`（手柄 SSAP 结构）
5. `[probe] recv len=NN <hex>`（若手柄推送数据）

- [ ] **Step 4: 记录结果**

将实测 log 关键段（seek 设备列表、SSAP 结构、recv 帧）整理保存，供 payload 格式分析。若某阶段失败，记录状态码与现象，回到对应 Task 排查。

- [ ] **Step 5: 更新项目文档**

按实测结果更新 `docs/bs21-development.md` 的 M7 章节（或新增探索记录），说明数据通道是否打通、SSAP 结构、payload 初步观察。

```bash
cd /home/bodong/workspace/flydigi-receiver/.worktrees/m7-sle-probe
git add -A
git commit -m "docs: record sle_probe on-board findings"
```

---

## 验证结果（2026-08-21，board_a 实测）

### 已完整验证 ✅
- 扫描发现：手柄 `a1:a2:c8:75:43:b8`，广播 payload 含 `fly_digig`（飞智标识），RSSI ≈ -50
- 连接：`[Connected]`，`conn state: 1`
- 配对：`pair complete: 0x0`
- MTU 交换：`mtu=520`
- SSAP 特征发现：handles 0x11~0x18
  - 0x11: oper_ind=781(0x30D) = READ|WRITE|NOTIFY|DESCRIPTOR_WRITE，desc=1 → 输入报告特征
  - 0x13: oper_ind=13(0x0D) = READ|WRITE_NO_RSP|NOTIFY
  - 0x12/0x14/0x16/0x17/0x18: 普通读写特征
- 开启通知：向 0x12~0x19 所有可能 CCC 描述符写 0x0001，全部 `write cfm status=0x0`

### 阻塞点 ❌
连接稳定（无断链）、通知开启成功，但**即使手柄持续移动摇杆/按键，仍收不到任何 notification/indication 数据**（0 条 `recv`）。

### 结论与下一步
飞智手柄在 SLE 2.4GHz 模式下，建立普通 SLE 连接后**不会主动推送输入数据**。
推断需要先完成一段**飞智私有握手/协议**（主机侧先发特定命令）才会开始上报输入报告。
这已超出"通用 SLE 数据接收"范畴，属**飞智私有协议逆向**任务，建议作为新里程碑（如 M8）。

sle_probe 当前代码（`apps/sle_probe/sle_probe_client.c`）已含暴力开启 0x12~0x19 通知的逻辑，
留作后续协议分析的基础工具。

---

## 协议分析补充（2026-08-21 续，m7-uuid-probe）

### 特征 UUID
所有 property（0x11~0x18）的 UUID 均为 **0xBE37**（小端 `37 be`，2 字节 UUID），
属飞智私有服务，**非标准 HID**。各特征仅靠 handle 区分。

### 读操作（ssapc_read_req）✅
向 0x11~0x18 读均成功，返回：
- `fly_digigs`（10 字节，手柄名）
- `MAGIC-103F-12D1-0001`（20 字节，型号串）
- 状态字节：`01`、`01 00`、`06 00`、`00 05 02` 等

⇒ 连接/SSAP 完全正常，控制器有响应，但读回的是**元数据，不是实时输入**。

### 通知/指示（recv）❌
- 向 0x12~0x19 所有可能 CCC 写 0x0001 开启通知，全部 cfm 成功
- 向可写特征（0x11~0x14）写命令 0x01 / 0x03 / 0x05 / 0x08 / 0x02
- 用户持续移动摇杆+按键
- 结果：**0 条 recv / ind 数据**

### 结论
飞智手柄在普通 SLE 连接下**不会主动上报输入报告**。简单命令猜测（0x01~0x08）无效，
说明需要一段**飞智私有握手/命令序列**才会进入流数据模式。read_cfm 的 handle 字段
恒为 0x0（SDK 回调不回显 handle），无法直接映射哪条数据来自哪个 handle。

**下一步 = 飞智私有协议逆向（建议立项 M8）**，方向：
1. 抓官方 2.4G 接收器的 SLE 空口通信做参考（需嗅探/逻辑分析仪）
2. 或先逆向其 BLE 模式报告格式（可能近似），再对照 SLE 模式
3. 系统遍历写命令组合 + 逐 handle 轮询读，定位输入报告特征与激活命令
