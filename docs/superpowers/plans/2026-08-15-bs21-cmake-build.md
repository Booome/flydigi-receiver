# BS21 非侵入式 CMake 构建 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用标准 CMake 流程（`cmake -S` + `cmake --build`）替换侵入式 `build.sh`，SDK 全程只读。

**Architecture:** 顶层 `CMakeLists.txt` 作为 cmake 入口，`execute_process` 调 `gen-config.py`（复用 SDK `TargetEnvironment`）派生 60 个构建参数、调 `usr_config.py` 生成 mconfig.h；`add_subdirectory` 绝对路径引入 SDK 组件 + 我们的 `app`/`sdk-compat` 组件；`add_custom_target` 调 `packet.py` 打包。

**Tech Stack:** CMake 3.14+, Python 3（复用 SDK 的 enviroment.py / usr_config.py / packet.py）, Ai-BS21_SDK（`~/.local/Ai-BS21_SDK`）, ws63flash。

## Global Constraints

- SDK 路径：`~/.local/Ai-BS21_SDK`（可用环境变量 `AI_BS21_SDK_PATH` 覆盖）
- SDK 全程只读：构建前后 `git -C ~/.local/Ai-BS21_SDK status` 必须无变化
- target 固定 `bs21-n1100-rcu`，宏注入 `NO_BOOT_BACKUP`（修复 flash 布局 bug）
- 所有代码注释英文；计划文档正文中文
- Python 用 pyenv 默认（3.14.6），已装 `setuptools`（提供 distutils）
- 烧录：`ws63flash --flash /dev/ttyUSB1 <fwpkg> -b460800`；串口 115200

---

### Task 1: 参数生成器 gen-config.py

**Files:**
- Create: `wireless/bs21/scripts/gen-config.py`

**Interfaces:**
- Produces: 命令行 `gen-config.py <SDK> <target> <output.cmake>`，向 `output.cmake` 写入 `set()` 语句（含 `CHIP`/`CORE`/`RAM_COMPONENT`/`DEFINES`/`CCFLAGS`/`LINKFLAGS`/`CMAKE_TOOLCHAIN_FILE`/`SECTOR_CFG` 等），供顶层 CMakeLists `include`。

- [ ] **Step 1: 写 gen-config.py**

```python
#!/usr/bin/env python3
# Generate CMake set() statements for the Ai-BS21_SDK target config.
# Reuses the SDK's TargetEnvironment (read-only) and emits a .cmake file.
import os
import sys

SDK, TARGET, OUT = sys.argv[1], sys.argv[2], sys.argv[3]
sys.path.append(os.path.join(SDK, 'build', 'script'))
sys.path.append(os.path.join(SDK, 'build', 'config'))
from enviroment import TargetEnvironment

env = TargetEnvironment(TARGET, extra_defines=['NO_BOOT_BACKUP'])
env.remove('ram_component', 'demo')
env.remove('ram_component', 'samples')
env.append('ram_component', 'app')
env.append('ram_component', 'sdk_compat')

if 'rom_sym_path' in env.config:
    env.config['rom_sym_path'] = env.config['rom_sym_path'].replace('<root>', SDK)
    if os.path.exists(env.config['rom_sym_path']):
        env.config['linkflags'].append('-Wl,--just-symbols=' + env.config['rom_sym_path'])
        env.config['linkflags'].extend(env.get('symlink_linkflags', cmake_type=False, default=[]))
        env.config['ccflags'].extend(env.get('symlink_ccflags', cmake_type=False, default=[]))
        env.config['defines'].append('ROM_SYMBOL_LINK')

with open(OUT, 'w') as f:
    f.write('# Auto-generated by gen-config.py. Do not edit.\n')
    for key in env.config:
        val = env.get(key)
        if val is None or val == '':
            continue
        f.write('set(%s "%s" CACHE INTERNAL "" FORCE)\n' % (key.upper(), val))
    f.write('set(CMAKE_TOOLCHAIN_FILE "%s")\n' % env.get_tool_chain())
```

- [ ] **Step 2: 运行并验证输出**

```bash
cd ~/.local/Ai-BS21_SDK
python3 /home/bodong/workspace/flydigi-receiver/.worktrees/bs21-dev/wireless/bs21/scripts/gen-config.py \
  ~/.local/Ai-BS21_SDK bs21-n1100-rcu /tmp/sdk-config.cmake
grep -c "RAM_COMPONENT" /tmp/sdk-config.cmake   # 期望 1
grep -o 'sdk_compat\|"app"' /tmp/sdk-config.cmake | sort -u   # 期望 app、sdk_compat 存在
grep -o '"demo"\|"samples"' /tmp/sdk-config.cmake || echo "OK: demo/samples 已移除"
```

Expected: `app`、`sdk_compat` 出现，`demo`、`samples` 不出现。

- [ ] **Step 3: 提交**

```bash
cd /home/bodong/workspace/flydigi-receiver/.worktrees/bs21-dev
git add wireless/bs21/scripts/gen-config.py
git commit -m "feat: add BS21 SDK config generator (reuse TargetEnvironment)"
```

---

### Task 2: app 与 sdk-compat 组件

**Files:**
- Create: `wireless/bs21/app/CMakeLists.txt`
- Create: `wireless/bs21/app/main.c`
- Create: `wireless/bs21/sdk-compat/CMakeLists.txt`
- Create: `wireless/bs21/sdk-compat/ble_stub.c`（从 `wireless/bs21/overlay/app/ble_stub.c` 迁移）

**Interfaces:**
- Produces: 组件名 `app`（`app_entry` + `hello_task`）、`sdk_compat`（36 个 `sapi_ble_*` stub）。两者都需在 `RAM_COMPONENT` 中（gen-config.py 已处理）。

- [ ] **Step 1: 写 app/main.c**

```c
#include "soc_osal.h"
#include "app_init.h"

static void *hello_task(const char *arg)
{
    (void)arg;
    while (1) {
        osal_printk("flydigi app running\r\n");
        osal_msleep(1000);
    }
    return NULL;
}

static void app_entry(void)
{
    osal_kthread_lock();
    osal_task *t = osal_kthread_create((osal_kthread_handler)hello_task, 0, "hello", 0x1000);
    if (t != NULL) {
        osal_kthread_set_priority(t, 24);
    }
    osal_kthread_unlock();
}

app_run(app_entry);
```

- [ ] **Step 2: 写 app/CMakeLists.txt**

```cmake
set(COMPONENT_NAME "app")

set(SOURCES
    ${CMAKE_CURRENT_SOURCE_DIR}/main.c
)

set(PUBLIC_HEADER
    ${CMAKE_CURRENT_SOURCE_DIR}
)

set(PRIVATE_HEADER)

set(PRIVATE_DEFINES)

set(PUBLIC_DEFINES)

set(COMPONENT_PUBLIC_CCFLAGS)

set(COMPONENT_CCFLAGS)

set(WHOLE_LINK
true
)

set(MAIN_COMPONENT
false
)

build_component()
```

- [ ] **Step 3: 写 sdk-compat/CMakeLists.txt**

```cmake
set(COMPONENT_NAME "sdk_compat")

set(SOURCES
    ${CMAKE_CURRENT_SOURCE_DIR}/ble_stub.c
)

set(PUBLIC_HEADER)

set(PRIVATE_HEADER)

set(PRIVATE_DEFINES)

set(PUBLIC_DEFINES)

set(COMPONENT_PUBLIC_CCFLAGS)

set(COMPONENT_CCFLAGS)

set(WHOLE_LINK
true
)

set(MAIN_COMPONENT
false
)

build_component()
```

- [ ] **Step 4: 迁移 ble_stub.c**

```bash
cd /home/bodong/workspace/flydigi-receiver/.worktrees/bs21-dev
mkdir -p wireless/bs21/sdk-compat
git mv wireless/bs21/overlay/app/ble_stub.c wireless/bs21/sdk-compat/ble_stub.c
```

- [ ] **Step 5: 提交**

```bash
git add wireless/bs21/app wireless/bs21/sdk-compat
git commit -m "feat: add app and sdk-compat components (replace SDK demo)"
```

---

### Task 3: 顶层 CMakeLists.txt

**Files:**
- Create: `wireless/bs21/CMakeLists.txt`

**Interfaces:**
- Consumes: `scripts/gen-config.py`（Task 1）、组件 `app`/`sdk_compat`（Task 2）、SDK 的 `build/cmake/*.cmake` 模块、`usr_config.py`、`packet.py`。
- Produces: configure 产物 `output/sdk-config.cmake` + `output/mconfig.h` + `output/application.elf`；build 产物 `output/*.fwpkg`。

- [ ] **Step 1: 写顶层 CMakeLists.txt**

```cmake
cmake_minimum_required(VERSION 3.14.1)
set(CMAKE_SYSTEM_NAME "Generic")

if(NOT DEFINED SDK_ROOT)
    if(DEFINED ENV{AI_BS21_SDK_PATH})
        set(SDK_ROOT "$ENV{AI_BS21_SDK_PATH}")
    else()
        set(SDK_ROOT "$ENV{HOME}/.local/Ai-BS21_SDK")
    endif()
endif()
if(NOT DEFINED BS21_TARGET)
    set(BS21_TARGET "bs21-n1100-rcu")
endif()
find_package(Python3 COMPONENTS Interpreter REQUIRED)

set(CONFIG_FILE "${CMAKE_CURRENT_BINARY_DIR}/sdk-config.cmake")
execute_process(
    COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/scripts/gen-config.py
            ${SDK_ROOT} ${BS21_TARGET} ${CONFIG_FILE}
    RESULT_VARIABLE gen_result)
if(NOT gen_result EQUAL 0)
    message(FATAL_ERROR "gen-config.py failed: ${gen_result}")
endif()
include(${CONFIG_FILE})

include(${SDK_ROOT}/build/cmake/build_function.cmake)
include(${SDK_ROOT}/build/cmake/global_variable.cmake)
include(${SDK_ROOT}/build/cmake/build_script.cmake)
include(${SDK_ROOT}/build/cmake/build_command.cmake)
include(${SDK_ROOT}/build/cmake/build_hso_database.cmake)
include(${SDK_ROOT}/build/cmake/build_component.cmake)
include(${SDK_ROOT}/build/cmake/build_sdk.cmake)

project(${CHIP}_CFBB C ASM CXX)

set(TARGET_COMPONENT "${RAM_COMPONENT}" "${ROM_COMPONENT}")
set(TARGET_NAME ${BIN_NAME})
file(WRITE ${PROJECT_BINARY_DIR}/temp/__null___.c "int __null___(void) {return 0;}")
add_executable(${BIN_NAME} ${PROJECT_BINARY_DIR}/temp/__null___.c)
set_target_properties(${BIN_NAME} PROPERTIES RUNTIME_OUTPUT_NAME ${BIN_NAME}.elf)
target_compile_options(${BIN_NAME} PRIVATE "${CCFLAGS}")

execute_process(
    COMMAND ${Python3_EXECUTABLE} ${SDK_ROOT}/build/script/usr_config.py
            savemenuconfig ${CHIP} ${CORE} ${BS21_TARGET} ${CMAKE_BINARY_DIR}
    WORKING_DIRECTORY ${SDK_ROOT}
    RESULT_VARIABLE mconfig_result)
if(NOT mconfig_result EQUAL 0)
    message(FATAL_ERROR "mconfig failed")
endif()

set(KCONFIG_PATH "${SDK_ROOT}/build/config/target_config/${CHIP}/menuconfig/${CORE}/${BS21_TARGET}.config")
if(EXISTS ${KCONFIG_PATH})
    KCONFIG_GET_PARAMS(${KCONFIG_PATH})
    set(USE_KCONFIG True)
endif()

add_path_if_exist(${SDK_ROOT}/application/bs21/standard)
add_path_if_exist(${SDK_ROOT}/kernel)
add_path_if_exist(${SDK_ROOT}/drivers)
add_path_if_exist(${SDK_ROOT}/middleware)
add_path_if_exist(${SDK_ROOT}/open_source)
add_subdirectory(app)
add_subdirectory(sdk-compat)

add_custom_target(package ALL
    COMMAND ${Python3_EXECUTABLE} ${SDK_ROOT}/tools/pkg/packet.py
            ${CHIP} ${BS21_TARGET} NO_BOOT_BACKUP ${SECTOR_CFG}
    WORKING_DIRECTORY ${SDK_ROOT}
    DEPENDS ${BIN_NAME})
```

- [ ] **Step 2: configure 验证**

```bash
cd /home/bodong/workspace/flydigi-receiver/.worktrees/bs21-dev
cmake -S wireless/bs21 -B output 2>&1 | tail -30
```

Expected: 无 `FATAL_ERROR`，`output/sdk-config.cmake` 和 `output/mconfig.h` 已生成。若报错，按错误信息修正 CMakeLists（此任务只验证 configure，不编译）。

- [ ] **Step 3: 提交**

```bash
git add wireless/bs21/CMakeLists.txt
git commit -m "feat: add top-level CMakeLists for read-only SDK build"
```

---

### Task 4: 端到端构建验证

**Files:**
- Modify: `wireless/bs21/CMakeLists.txt`（补充 SDK root 后半部分 + 编译修正）

- [ ] **Step 1: 补充顶层 CMakeLists 缺失的部分**

Task 3 的顶层 CMakeLists 漏了 SDK root CMakeLists 的后半部分（`project`/`add_subdirectory` 之后还有链接、签名、产 bin 等步骤）。这些是编译链接产 fwpkg 必需的。补充以下内容：

**1a. `include(${CONFIG_FILE})` 之后、7 个 `build/cmake` 模块 include 之前，补充 ROOT_DIR 等变量定义**（SDK root 用 `PROJECT_SOURCE_DIR` 推导，我们指向 SDK_ROOT）：

```cmake
set(ROOT_DIR "${SDK_ROOT}")
set(CMAKE_DIR "${SDK_ROOT}/build/cmake")
set(BIN_DIR "${SDK_ROOT}/interim_binary")
```

**1b. `project(...)` 之后补两行**：

```cmake
set(CMAKE_CXX_IMPLICIT_INCLUDE_DIRECTORIES "")
file(REMOVE ${CMAKE_BINARY_DIR}/auto_gen_libfunc.lds)
```

**1c. `add_subdirectory(sdk-compat)` 之后、`add_custom_target(package ...)` 之前，补充 SDK root 剩余序列**：

```cmake
add_path_if_exist(${SDK_ROOT}/protocol)
add_path_if_exist(${SDK_ROOT}/test)
add_path_if_exist(${SDK_ROOT}/include)
add_path_if_exist(${SDK_ROOT}/vendor)

include(${SDK_ROOT}/build/cmake/open_source.cmake)
include(${SDK_ROOT}/build/cmake/build_linker.cmake)

if (NOT DEFINED ROM_COMPONENT)
add_custom_target(GENERAT_BIN ALL
    COMMAND ${CMAKE_OBJCOPY} --gap-fill 0xFF -O binary -R .fls_loader_ram -R .logstr -R .ARM -R .ARM ${BIN_NAME}.elf ${BIN_NAME}.bin
    COMMENT "post_build:gen bin file"
    WORKING_DIRECTORY ${PROJECT_BINARY_DIR}
    DEPENDS ${TARGET_NAME}
)
else()
add_custom_target(GENERAT_BIN ALL
    COMMAND ${CMAKE_OBJCOPY} --gap-fill 0xFF -O binary -R .logstr -R .ARM.exidx -R .ARM.extab -R .*_romtext ${TARGET_NAME}.elf ${TARGET_NAME}.bin
    COMMAND ${CMAKE_OBJCOPY} --gap-fill 0xFF -O binary -j .*_romtext ${TARGET_NAME}.elf ${TARGET_NAME}_rom.bin
    COMMENT "post_build:gen rom and ram bin file"
    WORKING_DIRECTORY ${PROJECT_BINARY_DIR}
    DEPENDS ${TARGET_NAME}
)
endif()

include(${SDK_ROOT}/build/cmake/build_ssb.cmake)
include(${SDK_ROOT}/build/cmake/build_elf_info.cmake)
include(${SDK_ROOT}/build/cmake/build_sign.cmake)
include(${SDK_ROOT}/build/cmake/build_nv_bin.cmake)
include(${SDK_ROOT}/build/cmake/build_partition_bin.cmake)
include(${SDK_ROOT}/build/cmake/build_boot_bin_cp.cmake)

create_hso_db()
generate_project_file()
```

> 参考：SDK root `CMakeLists.txt` 第 22-24、51、53-55、63-66、82-118 行。这些 include 的 `build_*.cmake` 内部引用 `ROOT_DIR`/`CMAKE_DIR`/`BIN_DIR` 变量（指向 SDK 根），故 1a 的定义必须在它们之前。

- [ ] **Step 2: 重新 configure + 完整构建**

```bash
cd /home/bodong/workspace/flydigi-receiver/.worktrees/bs21-dev
cmake -S wireless/bs21 -B output 2>&1 | tail -30
cmake --build output -j 2>&1 | tail -30
```

Expected: configure 无 `FATAL_ERROR`，编译链接成功，`output/` 出现 `*.fwpkg`（`package` target 自动执行 packet.py）。

- [ ] **Step 3: 验证 SDK 只读**

```bash
git -C ~/.local/Ai-BS21_SDK status --short
```

Expected: 无**新增**输出（SDK 树有历史遗留的 chmod/config/pycache 修改，属先前 build.sh 时代；本任务不能新增）。

- [ ] **Step 4: 验证 _start 地址**

```bash
grep -nE "FLASH_STARTUP|__CODE_START__" output/application.map | head -3
```

Expected: `FLASH_STARTUP` 和 `__CODE_START__` 为 `0x9010b300`（NO_BOOT_BACKUP 生效）。注：新方案的 map 在 `output/`（不再是 SDK 的 output 目录）。

- [ ] **Step 5: 提交**

```bash
git add wireless/bs21/CMakeLists.txt
git commit -m "fix: complete CMakeLists with SDK root post-link sequence"
```

---

### Task 5: 烧录验证 + 迁移清理

**Files:**
- Delete: `wireless/bs21/scripts/build.sh`
- Delete: `wireless/bs21/overlay/`（`demo.c` 等，已被 app/ 取代）

- [ ] **Step 1: 烧录新固件**

```bash
ws63flash --flash /dev/ttyUSB1 /home/bodong/workspace/flydigi-receiver/.worktrees/bs21-dev/output/bs21_all_in_one.fwpkg -b460800
```

- [ ] **Step 2: 串口验证**

```bash
screen /dev/ttyUSB1 115200
```

Expected: 每 1 秒打印一行 `flydigi app running`。

- [ ] **Step 3: 删除旧构建脚本与 overlay**

```bash
cd /home/bodong/workspace/flydigi-receiver/.worktrees/bs21-dev
git rm wireless/bs21/scripts/build.sh
git rm -r wireless/bs21/overlay
```

（`setup-sdk.sh` 保留，纯环境准备。）

- [ ] **Step 4: 提交**

```bash
git add -A
git commit -m "refactor: switch to read-only CMake build, remove overlay/build.sh"
```

---

## Self-Review

**Spec coverage:**
- 目录结构（app/sdk-compat/scripts/gen-config.py/CMakeLists.txt）→ Task 1-3
- 构建流程（cmake -S -B + cmake --build）→ Task 3-4
- 参数生成（execute_process 调 gen-config.py）→ Task 1、3
- mconfig.h 生成（usr_config.py）→ Task 3
- 打包（packet.py custom target）→ Task 3
- SDK 只读验证 → Task 4 Step 2
- _start 地址验证 → Task 4 Step 3
- 烧录 + 打印验证 → Task 5
- 迁移删除 build.sh/overlay → Task 5

**Placeholder scan:** 无 TBD/TODO，所有代码块完整。

**Type consistency:** 组件名 `app`/`sdk_compat` 在 gen-config.py（append）与组件 CMakeLists（COMPONENT_NAME）一致；`gen-config.py` 命令行签名与顶层 CMakeLists 的 `execute_process` 一致。
