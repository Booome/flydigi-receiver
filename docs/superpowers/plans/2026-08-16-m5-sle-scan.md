# M5 SLE 扫描验证 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 BS21 编译出多工程固件（`default` / `g_scanner` / `t_broadcaster`），其中 `g_scanner` 能扫描并打印 SLE 广播（地址 + RSSI + 数据），为 M5 扫描手柄做准备。

**Architecture:** 顶层 `CMakeLists.txt` 通过 `-DBS21_APP=` 选择 `apps/` 下的工程；`gen-config.py` 按工程注册组件并切换角色（`g_scanner` 切 central 库 + `SUPPORT_SLE_CENTRAL`）。两个测试工程分别移植 SDK `sle_uart` 例程的扫描/广播逻辑。

**Tech Stack:** C（BS21 SDK 交叉编译）、CMake、Python（gen-config.py）、LiteOS（osal）。

## Global Constraints

- SDK 只读：`~/.local/Ai-BS21_SDK` 不得修改任何源码文件（`setup-sdk.sh` 的环境级处理除外）。
- 代码与注释用英文；`docs/` 下的文档用中文。
- 编译通过不算完成：每次代码变更后需用户烧录硬件验证运行正常。
- 工作在隔离 worktree：`/home/bodong/workspace/flydigi-receiver/.worktrees/m5-sle-scan`（分支 `m5-sle-scan`）。
- 构建命令（有 `-DBS21_APP=` 时选工程，无则默认 `default`）：
  - `cmake -S wireless/bs21 -B output` / `cmake --build output -j`
  - `cmake -S wireless/bs21 -B output/g_scanner -DBS21_APP=g_scanner` / `cmake --build output/g_scanner -j`
  - `cmake -S wireless/bs21 -B output/t_broadcaster -DBS21_APP=t_broadcaster` / `cmake --build output/t_broadcaster -j`
- 烧录：`ws63flash --flash /dev/ttyUSB1 <output 目录下生成的 fwpkg> -b460800`（先发命令再触发复位）；调试串口 `screen /dev/ttyUSB1 115200`。

---

## 文件结构

- Modify: `wireless/bs21/scripts/gen-config.py` — 增加 `APP` 参数（默认 `default`），按工程注册组件、切换 central 角色。
- Modify: `wireless/bs21/CMakeLists.txt` — 增加 `BS21_APP` 变量（默认 `default`），传给 gen-config.py，`add_subdirectory(apps/${BS21_APP})`。
- Move: `wireless/bs21/app/` → `wireless/bs21/apps/default/`（hello 测试迁移，`COMPONENT_NAME` 改为 `default`）。
- Create: `wireless/bs21/apps/g_scanner/CMakeLists.txt` + `main.c`（SLE 扫描器，central）。
- Create: `wireless/bs21/apps/t_broadcaster/CMakeLists.txt` + `main.c`（SLE 广播器，peripheral）。

关键 SDK 事实（实现时依赖）：
- `build_component()` 通过 `COMPONENT_NAME IN_LIST RAM_COMPONENT` 判断是否构建（`build_component.cmake:23`），故组件的 `COMPONENT_NAME` 必须等于 `gen-config.py` 里 `ram_component` 追加的名字。
- `bs21.cmake` / `bt_host.cmake` / `bth_gle.cmake` / `bt_app.cmake` 库选择：`if(DEFINED CONFIG_SLE_BLE_SUPPORT AND SUPPORT_MULTI_LIBS IN_LIST DEFINES)` 时取 `${PKG_CHIP}-${CONFIG_SLE_BLE_SUPPORT}/lib*.a`。`SUPPORT_MULTI_LIBS` 已由 `standard-bs21-n1100` 的 defines 提供；`CONFIG_SLE_BLE_SUPPORT` 是 CMake 变量，rcu 的 Kconfig 里未设置（不会被 `KCONFIG_GET_PARAMS` 覆盖），故由 gen-config.py 输出 `set(CONFIG_SLE_BLE_SUPPORT "sle-central")` 即可。
- 角色宏：`SUPPORT_SLE_PERIPHERAL`（rcu 默认，在 `config.py` 的 rcu defines 里）与 `SUPPORT_SLE_CENTRAL` 互斥。

---

### Task 1: 多工程基础设施 + default 工程迁移

**Files:**
- Modify: `wireless/bs21/scripts/gen-config.py`
- Modify: `wireless/bs21/CMakeLists.txt`
- Move: `wireless/bs21/app/CMakeLists.txt` → `wireless/bs21/apps/default/CMakeLists.txt`
- Move: `wireless/bs21/app/main.c` → `wireless/bs21/apps/default/main.c`

**Interfaces:**
- Produces: `gen-config.py` 接收第 4 个位置参数 `APP`（缺省 `default`）；`CMakeLists.txt` 提供缓存变量 `BS21_APP`（缺省 `default`）。后续任务（g_scanner / t_broadcaster）依赖这两处入口。

- [ ] **Step 1: 迁移 app 目录为 apps/default 并改组件名**

在 worktree 根目录执行：

```bash
git mv wireless/bs21/app wireless/bs21/apps
git mv wireless/bs21/apps/default/CMakeLists.txt /tmp/unused 2>/dev/null || true
mkdir -p wireless/bs21/apps/default
git mv wireless/bs21/apps/CMakeLists.txt wireless/bs21/apps/default/CMakeLists.txt
git mv wireless/bs21/apps/main.c wireless/bs21/apps/default/main.c
```

（若上面 `git mv` 顺序因目录已存在而报错，改用：先 `mkdir -p wireless/bs21/apps`，再 `git mv wireless/bs21/app/* wireless/bs21/apps/default/`，最后 `rmdir wireless/bs21/app`。）

把 `wireless/bs21/apps/default/CMakeLists.txt` 第 1 行改为：

```cmake
set(COMPONENT_NAME "default")
```

- [ ] **Step 2: 改造 gen-config.py**

编辑 `wireless/bs21/scripts/gen-config.py`，改动如下：

第 7 行（参数解包）改为：

```python
SDK, TARGET, OUT = sys.argv[1], sys.argv[2], sys.argv[3]
APP = sys.argv[4] if len(sys.argv) > 4 else 'default'
```

第 15 行（`env.append('ram_component', 'app')`）改为：

```python
env.append('ram_component', APP)
```

在第 16 行（`env.append('ram_component', 'sdk_compat')`）之后插入：

```python
if APP == 'g_scanner':
    env.remove('defines', 'SUPPORT_SLE_PERIPHERAL')
    env.append('defines', 'SUPPORT_SLE_CENTRAL')
    env.config['config_sle_ble_support'] = 'sle-central'
```

（`env.config['config_sle_ble_support'] = 'sle-central'` 经 dump 循环的 `key.upper()` 输出 `set(CONFIG_SLE_BLE_SUPPORT "sle-central" CACHE INTERNAL "" FORCE)`，触发 bs21.cmake 选 central 库。）

- [ ] **Step 3: 改造顶层 CMakeLists.txt**

编辑 `wireless/bs21/CMakeLists.txt`：

在第 13 行（`set(BS21_TARGET ...)` 的 `endif()` 之后、`find_package(Python3 ...)` 之前）插入：

```cmake
if(NOT DEFINED BS21_APP)
    set(BS21_APP "default")
endif()
```

第 24 行 gen-config.py 调用改为传第 4 个参数：

```cmake
    COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/scripts/gen-config.py
            ${SDK_ROOT} ${BS21_TARGET} ${CONFIG_FILE} ${BS21_APP}
```

第 95 行 `add_subdirectory(app)` 改为：

```cmake
add_subdirectory(apps/${BS21_APP})
```

- [ ] **Step 4: 编译 default 工程验证**

```bash
cmake -S wireless/bs21 -B output
cmake --build output -j
```

预期：编译成功，行为与改造前 hello 测试一致（固件仍打印 `flydigi app running`）。若无报错即通过。

- [ ] **Step 5: Commit**

```bash
git add wireless/bs21
git commit -m "build: add multi-app layout with default app"
```

---

### Task 2: g_scanner 扫描器工程（central）

**Files:**
- Create: `wireless/bs21/apps/g_scanner/CMakeLists.txt`
- Create: `wireless/bs21/apps/g_scanner/main.c`

**Interfaces:**
- Consumes: `BS21_APP=g_scanner` 时 gen-config.py 切换 central（Task 1）。
- Produces: `g_scanner` 固件，SLE 扫描并打印 `adv <addr> rssi:<n>` + 数据 hex。

- [ ] **Step 1: 写组件 CMakeLists.txt**

创建 `wireless/bs21/apps/g_scanner/CMakeLists.txt`：

```cmake
set(COMPONENT_NAME "g_scanner")

set(SOURCES
    ${CMAKE_CURRENT_SOURCE_DIR}/main.c
)

set(PUBLIC_HEADER
    ${CMAKE_CURRENT_SOURCE_DIR}
)

set(PRIVATE_HEADER)

set(PRIVATE_DEFINES)

set(PUBLIC_DEFINES)

set(COMPONENT_PUBLIC_CCFLAGS)

set(COMPONENT_CCFLAGS)

set(WHOLE_LINK
true
)

set(MAIN_COMPONENT
false
)

build_component()
```

- [ ] **Step 2: 写扫描器 main.c**

创建 `wireless/bs21/apps/g_scanner/main.c`（移植 `sle_uart_client.c` 的扫描部分，去掉连接/配对）：

```c
#include "soc_osal.h"
#include "pinctrl.h"
#include "gpio.h"
#include "chip_io.h"
#include "errcode.h"
#include "sle_device_manager.h"
#include "sle_device_discovery.h"

static sle_dev_manager_callbacks_t g_dev_cbk = { 0 };
static sle_announce_seek_callbacks_t g_seek_cbk = { 0 };

static void bs21_rst(void)
{
    uapi_pin_set_mode(S_MGPIO21, (pin_mode_t)HAL_PIO_FUNC_GPIO);
    uapi_gpio_set_dir(S_MGPIO21, GPIO_DIRECTION_INPUT);
    uapi_pin_set_pull(S_MGPIO21, PIN_PULL_UP);
    reg16_setbits(0x5702C51C, 4, 5, 21);
    reg16_clrbit(0x5702C51C, 0);
}

static void scan_start(void)
{
    sle_seek_param_t param = { 0 };
    errcode_t rc;
    param.own_addr_type = 0;
    param.filter_duplicates = 0;
    param.seek_filter_policy = 0;
    param.seek_phys = 1;
    param.seek_type[0] = 1;
    param.seek_interval[0] = 100;
    param.seek_window[0] = 100;
    rc = sle_set_seek_param(&param);
    if (rc != ERRCODE_SUCC) {
        osal_printk("sle_set_seek_param fail: 0x%x\r\n", rc);
        return;
    }
    rc = sle_start_seek();
    if (rc != ERRCODE_SUCC) {
        osal_printk("sle_start_seek fail: 0x%x\r\n", rc);
    }
}

static void sle_power_on_cb(uint8_t status)
{
    osal_printk("sle power on: %d\r\n", status);
    enable_sle();
}

static void sle_enable_cb(uint8_t status)
{
    osal_printk("sle enable: %d\r\n", status);
    sle_announce_seek_register_callbacks(&g_seek_cbk);
    scan_start();
}

static void seek_enable_cb(errcode_t status)
{
    osal_printk("seek enable: 0x%x\r\n", status);
}

static void seek_result_cb(sle_seek_result_info_t *result)
{
    if (result == NULL) {
        return;
    }
    osal_printk("adv %02x:%02x:%02x:%02x:%02x:%02x rssi:%d\r\n",
                result->addr.addr[0], result->addr.addr[1], result->addr.addr[2],
                result->addr.addr[3], result->addr.addr[4], result->addr.addr[5],
                result->rssi);
    for (uint8_t i = 0; i < result->data_length; i++) {
        osal_printk("%02x ", result->data[i]);
    }
    osal_printk("\r\n");
}

void axk_main(void)
{
    bs21_rst();

    g_dev_cbk.sle_power_on_cb = sle_power_on_cb;
    g_dev_cbk.sle_enable_cb = sle_enable_cb;
    sle_dev_manager_register_callbacks(&g_dev_cbk);

    g_seek_cbk.seek_enable_cb = seek_enable_cb;
    g_seek_cbk.seek_result_cb = seek_result_cb;

    enable_sle();
}
```

- [ ] **Step 3: 编译 g_scanner 验证 central 库链接**

```bash
cmake -S wireless/bs21 -B output/g_scanner -DBS21_APP=g_scanner
cmake --build output/g_scanner -j
```

预期：编译成功，蓝牙组件链接 `bs21-n1100-sle-central/` 下的库。若出现 `sapi_ble_*` 等未定义符号（central 库残留符号差异），按错误信息在 `sdk-compat/ble_stub.c` 补充空实现（此时需在 commit message 里说明）。

- [ ] **Step 4: Commit**

```bash
git add wireless/bs21/apps/g_scanner
git commit -m "feat: add g_scanner SLE central scan app"
```

---

### Task 3: t_broadcaster 广播器工程（peripheral）

**Files:**
- Create: `wireless/bs21/apps/t_broadcaster/CMakeLists.txt`
- Create: `wireless/bs21/apps/t_broadcaster/main.c`

**Interfaces:**
- Consumes: `BS21_APP=t_broadcaster` 时 gen-config.py 保持 peripheral（Task 1）。
- Produces: `t_broadcaster` 固件，SLE 广播（name `flydigi_t`）。

- [ ] **Step 1: 写组件 CMakeLists.txt**

创建 `wireless/bs21/apps/t_broadcaster/CMakeLists.txt`：

```cmake
set(COMPONENT_NAME "t_broadcaster")

set(SOURCES
    ${CMAKE_CURRENT_SOURCE_DIR}/main.c
)

set(PUBLIC_HEADER
    ${CMAKE_CURRENT_SOURCE_DIR}
)

set(PRIVATE_HEADER)

set(PRIVATE_DEFINES)

set(PUBLIC_DEFINES)

set(COMPONENT_PUBLIC_CCFLAGS)

set(COMPONENT_CCFLAGS)

set(WHOLE_LINK
true
)

set(MAIN_COMPONENT
false
)

build_component()
```

- [ ] **Step 2: 写广播器 main.c**

创建 `wireless/bs21/apps/t_broadcaster/main.c`（移植 `sle_uart_server_adv.c` 的广播部分，去掉 server service / 连接；`SLE_ADV_DATA_TYPE_*`、`sle_adv_common_value` 原定义在例程私有头，需内联）：

```c
#include "soc_osal.h"
#include "pinctrl.h"
#include "gpio.h"
#include "chip_io.h"
#include "securec.h"
#include "errcode.h"
#include "sle_common.h"
#include "sle_device_manager.h"
#include "sle_device_discovery.h"

#define ADV_NAME             "flydigi_t"
#define ADV_HANDLE           1
#define ADV_DATA_LEN_MAX     251
#define ADV_INTERVAL_MIN     0xC8
#define ADV_INTERVAL_MAX     0xC8
#define CONN_INTV_MIN        0x64
#define CONN_INTV_MAX        0x64
#define CONN_MAX_LATENCY     0x1F3
#define CONN_SUPERVISION_TO  0x1F4

#define ADV_CHANNEL_MAP_DEFAULT            0x07
#define ADV_DATA_TYPE_DISCOVERY_LEVEL      0x01
#define ADV_DATA_TYPE_ACCESS_MODE          0x02
#define ADV_DATA_TYPE_COMPLETE_LOCAL_NAME  0x0B

struct adv_common_value {
    uint8_t type;
    uint8_t length;
    uint8_t value;
};

static sle_dev_manager_callbacks_t g_dev_cbk = { 0 };
static sle_announce_seek_callbacks_t g_seek_cbk = { 0 };

static void bs21_rst(void)
{
    uapi_pin_set_mode(S_MGPIO21, (pin_mode_t)HAL_PIO_FUNC_GPIO);
    uapi_gpio_set_dir(S_MGPIO21, GPIO_DIRECTION_INPUT);
    uapi_pin_set_pull(S_MGPIO21, PIN_PULL_UP);
    reg16_setbits(0x5702C51C, 4, 5, 21);
    reg16_clrbit(0x5702C51C, 0);
}

static int set_announce_param(void)
{
    sle_announce_param_t param = { 0 };
    unsigned char local_addr[SLE_ADDR_LEN] = { 0 };

    param.announce_mode = SLE_ANNOUNCE_MODE_CONNECTABLE_SCANABLE;
    param.announce_handle = ADV_HANDLE;
    param.announce_gt_role = SLE_ANNOUNCE_ROLE_T_CAN_NEGO;
    param.announce_level = SLE_ANNOUNCE_LEVEL_NORMAL;
    param.announce_channel_map = ADV_CHANNEL_MAP_DEFAULT;
    param.announce_interval_min = ADV_INTERVAL_MIN;
    param.announce_interval_max = ADV_INTERVAL_MAX;
    param.conn_interval_min = CONN_INTV_MIN;
    param.conn_interval_max = CONN_INTV_MAX;
    param.conn_max_latency = CONN_MAX_LATENCY;
    param.conn_supervision_timeout = CONN_SUPERVISION_TO;
    param.own_addr.type = 0;
    if (memcpy_s(param.own_addr.addr, SLE_ADDR_LEN, local_addr, SLE_ADDR_LEN) != EOK) {
        return -1;
    }
    return sle_set_announce_param(param.announce_handle, &param);
}

static int set_announce_data(void)
{
    sle_announce_data_t data = { 0 };
    uint8_t announce_data[ADV_DATA_LEN_MAX] = { 0 };
    uint16_t idx = 0;
    const char *name = ADV_NAME;
    uint8_t name_len = sizeof(ADV_NAME) - 1;

    struct adv_common_value disc_level = {
        .type = ADV_DATA_TYPE_DISCOVERY_LEVEL,
        .length = sizeof(struct adv_common_value) - 1,
        .value = SLE_ANNOUNCE_LEVEL_NORMAL,
    };
    if (memcpy_s(&announce_data[idx], ADV_DATA_LEN_MAX - idx,
                 &disc_level, sizeof(disc_level)) != EOK) {
        return -1;
    }
    idx += sizeof(disc_level);

    struct adv_common_value access_mode = {
        .type = ADV_DATA_TYPE_ACCESS_MODE,
        .length = sizeof(struct adv_common_value) - 1,
        .value = 0,
    };
    if (memcpy_s(&announce_data[idx], ADV_DATA_LEN_MAX - idx,
                 &access_mode, sizeof(access_mode)) != EOK) {
        return -1;
    }
    idx += sizeof(access_mode);

    announce_data[idx++] = name_len + 1;
    announce_data[idx++] = ADV_DATA_TYPE_COMPLETE_LOCAL_NAME;
    if (memcpy_s(&announce_data[idx], ADV_DATA_LEN_MAX - idx, name, name_len) != EOK) {
        return -1;
    }
    idx += name_len;

    data.announce_data = announce_data;
    data.announce_data_len = idx;

    return sle_set_announce_data(ADV_HANDLE, &data);
}

static void announce_enable_cb(uint32_t announce_id, errcode_t status)
{
    osal_printk("announce enable id:%x status:0x%x\r\n", announce_id, status);
}

static void sle_power_on_cb(uint8_t status)
{
    osal_printk("sle power on: %d\r\n", status);
    enable_sle();
}

static void sle_enable_cb(uint8_t status)
{
    int rc;
    errcode_t announce_rc;
    osal_printk("sle enable: %d\r\n", status);
    sle_announce_seek_register_callbacks(&g_seek_cbk);
    rc = set_announce_param();
    if (rc != 0) {
        osal_printk("set_announce_param fail: %d\r\n", rc);
        return;
    }
    rc = set_announce_data();
    if (rc != 0) {
        osal_printk("set_announce_data fail: %d\r\n", rc);
        return;
    }
    announce_rc = sle_start_announce(ADV_HANDLE);
    if (announce_rc != ERRCODE_SUCC) {
        osal_printk("sle_start_announce fail: 0x%x\r\n", announce_rc);
    }
}

void axk_main(void)
{
    bs21_rst();

    g_dev_cbk.sle_power_on_cb = sle_power_on_cb;
    g_dev_cbk.sle_enable_cb = sle_enable_cb;
    sle_dev_manager_register_callbacks(&g_dev_cbk);

    g_seek_cbk.announce_enable_cb = announce_enable_cb;

    enable_sle();
}
```

- [ ] **Step 3: 编译 t_broadcaster 验证 peripheral 库链接**

```bash
cmake -S wireless/bs21 -B output/t_broadcaster -DBS21_APP=t_broadcaster
cmake --build output/t_broadcaster -j
```

预期：编译成功，peripheral 库正常链接。

- [ ] **Step 4: Commit**

```bash
git add wireless/bs21/apps/t_broadcaster
git commit -m "feat: add t_broadcaster SLE peripheral announce app"
```

---

### Task 4: 烧录与硬件验证（用户执行）

**Files:** 无（仅硬件操作）。

**Interfaces:**
- Consumes: Task 2 的 `g_scanner` 固件、Task 3 的 `t_broadcaster` 固件。

- [ ] **Step 1: 烧录 t_broadcaster 到 Kit A**

```bash
ws63flash --flash /dev/ttyUSB1 <output/t_broadcaster 下生成的 fwpkg> -b460800
```

（先运行命令，出现等待提示后再给开发板复位/上电。）

- [ ] **Step 2: 烧录 g_scanner 到 Kit B**

```bash
ws63flash --flash /dev/ttyUSB1 <output/g_scanner 下生成的 fwpkg> -b460800
```

- [ ] **Step 3: 观察 g_scanner 串口（互通验证）**

```bash
screen /dev/ttyUSB1 115200
```

预期：`g_scanner` 持续打印 `adv <addr> rssi:<n>` 及数据 hex，其中应包含 `t_broadcaster` 的广播（数据 hex 里可见 `0b 0a 66 6c 79 64 69 67 69 5f 74` = name `flydigi_t`）。确认 G 节点能扫到 T 节点广播。

- [ ] **Step 4: 扫描手柄（M5 成功标准）**

手柄开机（2.4G 模式），`g_scanner` 打印手柄广播（拿到地址 + 广播数据 + RSSI），达成 M5 成功标准。将观察到的手柄地址与广播数据 hex 记录下来，供后续 M6（连接/配对）使用。
