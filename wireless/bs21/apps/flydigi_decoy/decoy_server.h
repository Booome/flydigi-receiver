#ifndef FLYDIGI_DECOY_SERVER_H
#define FLYDIGI_DECOY_SERVER_H

#include <stdint.h>

/* Register the mirrored SSAP server (call after SLE is enabled). */
void decoy_server_init(void);

#endif /* FLYDIGI_DECOY_SERVER_H */
